package dk.carp.gardener.authentication.core.authorization.datasource

/**
 * Enum class that notes the authorization mechanism used
 * by the third-party vendor.
 */
enum class AuthorizationType {
    OAUTH1,
    OAUTH2
}